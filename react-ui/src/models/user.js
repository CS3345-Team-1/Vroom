export class User {
    constructor(id, email, firstName, lastName, meetings, groups) {
        this.id = id
        this.email = email
        this.firstName = firstName
        this.lastName = lastName
        this.meetings = meetings
        this.groups = groups
    }
}